{
  "name": "@tokencap/web",
  "version": "0.1.0",
  "description": "Tokencap landing page",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp src/index.html dist/ && cp src/styles.css dist/",
    "dev": "npx serve src",
    "preview": "npx serve dist"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
